name: Check for updates of autogenerated wordlists

on:
  push:
  schedule:
    - cron: '0 10 * * *'

jobs:
  check-for-updates:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: scripts/update-all
      - run: git diff --exit-code wordlists/
